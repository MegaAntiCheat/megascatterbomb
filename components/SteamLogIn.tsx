"use client";

import React from "react";

const SteamLogin = () => {
  return (
    <button
      onClick={() => {
        console.log("Log in with steam");
      }}
    >
      Log in with Steam
    </button>
  );
};

export { SteamLogin };
